import { styled } from '@stitches/react'

export const _Footer = styled('footer', {
  position: 'fixed',
  bottom: 0,
  width: '100%',
  textAlign: 'center',
  padding: '2rem',
  fontSize: '0.8rem',
})
