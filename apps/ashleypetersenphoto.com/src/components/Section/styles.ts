import { styled } from '@/config/stitches.config'

export const _Section = styled('section', {
  position: 'relative',
  display: 'flex',
  justifyContent: `center`,
  width: `100%`,
  padding: `1rem`
})
