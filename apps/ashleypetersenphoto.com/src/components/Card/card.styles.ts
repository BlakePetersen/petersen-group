import { styled } from '@/config/stitches.config'

export const _Card = styled('div', {
  borderRadius: '.25rem',
  overflow: 'hidden',
  position: `relative`,
  display: 'block',
  boxShadow: '0 3px 6px 0 $grayA6'
})
