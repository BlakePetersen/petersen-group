import { styled } from '@/config/stitches.config'

export const _Frame = styled('main', {
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  backgroundColor: '$gray1',
  width: `100vw`,
  padding: `1.5rem`,
  borderRadius: `1rem`
})
