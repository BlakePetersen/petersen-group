import { styled } from '@stitches/react'

export const _Section = styled('section', {
  position: 'relative',
  display: 'flex',
  width: `100%`,
  maxWidth: 1600,
  borderRadius: '0.5rem',
  padding: `1rem`
})
