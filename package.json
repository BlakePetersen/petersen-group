{
  "name": "petersen-group",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "start": "turbo run start --parallel",
    "studio:ashleypetersenphoto": "turbo run sanity start --scope=studio.ashleypetersenphoto.com",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.4",
    "prettier": "^2.5.1",
    "turbo": "^1.0.28"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "dist/**",
          ".next/**"
        ]
      },
      "clean": {},
      "dev": {
        "cache": false
      },
      "lint": {},
      "start": {
        "dependsOn": [
          "^build"
        ]
      },
      "sanity": {},
      "test": {}
    }
  },
  "lint-staged": {
    "{apps,packages}/ui/**/*.{js,ts,jsx,tsx}": [
      "yarn eslint --quiet --fix",
      "bash -c tsc --noEmit"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
